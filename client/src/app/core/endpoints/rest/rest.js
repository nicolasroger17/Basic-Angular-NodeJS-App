(function (angular, _) { 'use strict';

    /**
     * Rest service.
     */
    angular.module('tennis.core.endpoints.rest', [])
        .service('rest', function ($rootScope) {
            var rest = this;
        });
})(window.angular, window._);
