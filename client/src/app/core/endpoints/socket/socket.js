(function (angular, _) { 'use strict';

    /**
     * Socket service.
     */
    angular.module('tennis.core.endpoints.socket', [])
        .service('Socket', function ($rootScope) {
            var socket = this;

        });
})(window.angular, window._);
